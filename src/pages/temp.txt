<h1>Block</h1>
      {isInitializing ? (
        <>
          <p>Initializing...</p>
        </>
      ) : (
        <div>
          <p>Block Number:{blockNumber}</p>
          <p>
            Nonce:{" "}
            <input
              type="text"
              value={nonce.toString()}
              onChange={handleNounceChange}
            />
          </p>
          <p>
            Data:{" "}
            <input
              type="text"
              value={data}
              onChange={(e) => setData(e.target.value)}
            />
          </p>
          <p>Hash: {hash}</p>
          <p>
            <button onClick={handleMineClick}>
              {mining ? "Stop Mining" : "Start Mining"}
            </button>
          </p>
        </div>
      )}